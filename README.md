> _đây là demo về cách sử dụng React một cách tối thiểu cho static web_
> Target: to have an **interactivity element** inside an **`existing page`**
> Solution: create an **React component**

================================================================